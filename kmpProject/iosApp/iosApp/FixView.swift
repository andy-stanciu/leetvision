import SwiftUI

struct FixView: View {
    var body: some View {
        VStack {
            Text("Fix View")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
        .navigationBarTitle("Fix", displayMode: .inline)
    }
}
