import SwiftUI

struct ExplainView: View {
    var body: some View {
        VStack {
            Text("Explain View")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
        .navigationBarTitle("Explain", displayMode: .inline)
    }
}
