from setuptools import setup, find_packages

setup(
    name="solution_recognizer",
    version="0.1",
    packages=find_packages(),
)

setup(
    name="model",
    version="0.1",
    packages=find_packages(),
)